# OMNIVISTA AI backend
# Minimum-version ranges (not exact pins) so pip can resolve a mutually
# compatible set on the target Python (3.11 on Render/Vercel). Adding a new
# dep: use a lower bound, not ==, to avoid hard conflicts.

# --- Web framework -----------------------------------------------------------
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# --- Document parsing ---------------------------------------------------------
pypdf>=4.0
pdfplumber>=0.10
python-docx>=1.0
python-pptx>=0.6.23
reportlab>=4.0

# --- Imagery / charts ---------------------------------------------------------
matplotlib>=3.8
numpy>=1.26
pillow>=10.0

# --- HTTP ---------------------------------------------------------------------
httpx>=0.27

# --- Testing -------------------------------------------------------------------
pytest>=8.2,<9          # pytest-asyncio requires pytest<9
pytest-asyncio>=0.24,<2
